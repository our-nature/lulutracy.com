---
allowed-tools: Bash(make:*)
description: Run all validation checks (typecheck, lint, format, test)
---

# Validate Command

Run the full validation suite to ensure code quality.

## Task

Execute `make validate` and report any issues found. If issues are found, provide specific guidance on how to fix them.
